# Site images

Move the existing images into this folder:

```text
assets/images/profile.jpg
assets/images/kirby1.png
```

`profile.jpg` is used by the home/about pages and social cards. `kirby1.png` is used as the favicon.
